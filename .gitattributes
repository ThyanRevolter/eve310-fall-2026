# Normalize line endings so Windows, macOS, and WSL students see identical diffs
* text=auto eol=lf

*.bat text eol=crlf
*.ps1 text eol=crlf

# Treat notebooks as text but avoid noisy merges
*.ipynb text

# Binary course material
*.pdf binary
*.pptx binary
*.png binary
*.jpg binary
*.xlsx binary
